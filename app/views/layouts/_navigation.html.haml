= link_to "Donez", root_path, :class => 'brand'
%ul.nav
  %li= link_to 'Puncte de colectare', home_path(:centre)
  %li= link_to 'Întrebări frecvente', home_path(:faq)
%ul.nav.pull-right
  - if user_signed_in?
    %li
      = link_to 'Profil', edit_user_registration_path
  - else
    %li
      = link_to 'Registrare', new_user_registration_path
  - if user_signed_in?
    %li
      = link_to 'Ieşire', destroy_user_session_path, :method=>'delete'
  - else
    %li
      = link_to 'Logare', new_user_session_path
